"use client";
import { Card } from "@/components/ui/Card"; import { EmptyState } from "@/components/ui/EmptyState"; import { Badge } from "@/components/ui/Badge"; import type { AcademicYear } from "@/types/database"; import { AcademicYearActions } from "./AcademicYearActions";
export function AcademicYearRecords({years}:{years:AcademicYear[]}){if(!years.length)return <EmptyState title="沒有符合條件的學年度" description="可新增第一個學年度。"/>;return <div className="grid gap-3">{years.map(year=><Card key={year.id} className="flex flex-col gap-3 p-4 sm:flex-row sm:items-center sm:justify-between"><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><h2 className="font-semibold">{year.year} 學年度</h2>{year.is_current&&<Badge tone="info">目前</Badge>}</div><p className="text-sm text-(--muted)">{year.start_date.slice(0,10)} 至 {year.end_date.slice(0,10)}</p></div><div className="flex shrink-0 flex-wrap gap-2"><AcademicYearActions year={year}/></div></Card>)}</div>}
