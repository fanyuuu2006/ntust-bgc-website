"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { ConfirmDialog } from "@/components/ConfirmDialog";
import { Modal } from "@/components/Modal";
import { apiClient } from "@/libs/api/client";
import type { AcademicYear } from "@/types/database";
const emptyValues = { year: "", start_date: "", end_date: "" };
export function AcademicYearManager({ years }: { years: AcademicYear[] }) { const router = useRouter(); const [editing, setEditing] = useState<AcademicYear | "new" | null>(null); const [confirmingCurrent, setConfirmingCurrent] = useState<AcademicYear | null>(null); const [deleting, setDeleting] = useState<AcademicYear | null>(null); const [values, setValues] = useState(emptyValues); const [message, setMessage] = useState<string | null>(null); const [busy, setBusy] = useState(false); const openCreate = () => { setValues(emptyValues); setEditing("new"); }; const openEdit = (year: AcademicYear) => { setValues({ year: year.year, start_date: year.start_date.slice(0, 10), end_date: year.end_date.slice(0, 10) }); setEditing(year); }; const setValue = (key: keyof typeof values, value: string) => setValues((current) => ({ ...current, [key]: value })); const save = async (event: React.FormEvent) => { event.preventDefault(); if (!editing) return; setBusy(true); setMessage(null); try { await apiClient(editing === "new" ? "/api/admin/academic-years" : `/api/admin/academic-years/${editing.id}`, { method: editing === "new" ? "POST" : "PATCH", body: values }); setMessage(`學年度已${editing === "new" ? "新增" : "更新"}。`); setEditing(null); router.refresh(); } catch (error) { setMessage(error instanceof Error ? error.message : "操作失敗，請稍後再試。"); } finally { setBusy(false); } }; const setCurrent = async () => { if (!confirmingCurrent) return; setBusy(true); try { await apiClient(`/api/admin/academic-years/${confirmingCurrent.id}`, { method: "PATCH", body: { action: "set-current" } }); setMessage("目前學年度已更新。"); setConfirmingCurrent(null); router.refresh(); } catch (error) { setMessage(error instanceof Error ? error.message : "更新失敗"); } finally { setBusy(false); } }; const remove = async () => { if (!deleting) return; setBusy(true); try { await apiClient(`/api/admin/academic-years/${deleting.id}`, { method: "DELETE" }); setMessage("學年度已刪除。"); setDeleting(null); router.refresh(); } catch (error) { setMessage(error instanceof Error ? error.message : "刪除失敗"); } finally { setBusy(false); } }; return <div className="space-y-4"><div className="flex justify-end"><button type="button" onClick={openCreate} className="btn primary rounded-lg px-4 py-2.5 text-sm font-medium">+ 新增學年度</button></div>{message && <p role="status" className="rounded-lg border border-(--border) bg-(--secondary-background) px-3 py-2 text-sm">{message}</p>}<div className="grid gap-3">{years.map((year) => <article key={year.id} className="card flex flex-col gap-3 rounded-xl p-4 sm:flex-row sm:items-center sm:justify-between"><div><div className="flex items-center gap-2"><h2 className="font-bold">{year.year} 學年度</h2>{year.is_current && <span className="rounded-full bg-(--primary) px-2 py-0.5 text-xs text-white">目前學年度</span>}</div><p className="mt-1 text-sm text-(--muted)">{year.start_date.slice(0, 10)} 至 {year.end_date.slice(0, 10)}</p></div><div className="flex flex-wrap gap-2"><button disabled={busy || year.is_current} onClick={() => setConfirmingCurrent(year)} className="btn primary rounded-lg px-3 py-2 text-sm">設為目前</button><button disabled={busy} onClick={() => openEdit(year)} className="btn outline rounded-lg px-3 py-2 text-sm">編輯</button><button disabled={busy || year.is_current} onClick={() => setDeleting(year)} className="btn danger rounded-lg px-3 py-2 text-sm">刪除</button></div></article>)}</div><Modal open={editing !== null} onClose={() => { if (!busy) setEditing(null); }} title={editing === "new" ? "新增學年度" : "編輯學年度"}><form onSubmit={save} className="space-y-4"><label className="block text-sm font-medium">學年度<input autoFocus required pattern="\d{3}" value={values.year} onChange={(event) => setValue("year", event.target.value)} placeholder="例如：115" className="mt-1.5 w-full rounded-lg border border-(--border) px-3 py-2.5 font-normal" /></label><label className="block text-sm font-medium">開始日期<input required type="date" value={values.start_date} onChange={(event) => setValue("start_date", event.target.value)} className="mt-1.5 w-full rounded-lg border border-(--border) px-3 py-2.5 font-normal" /></label><label className="block text-sm font-medium">結束日期<input required type="date" value={values.end_date} onChange={(event) => setValue("end_date", event.target.value)} className="mt-1.5 w-full rounded-lg border border-(--border) px-3 py-2.5 font-normal" /></label><div className="flex justify-end gap-2"><button type="button" disabled={busy} onClick={() => setEditing(null)} className="btn outline rounded-lg px-4 py-2 text-sm">取消</button><button disabled={busy} className="btn primary rounded-lg px-4 py-2 text-sm">{busy ? "儲存中…" : "儲存"}</button></div></form></Modal><ConfirmDialog open={confirmingCurrent !== null} onClose={() => { if (!busy) setConfirmingCurrent(null); }} onConfirm={setCurrent} isSubmitting={busy} title="設定目前學年度？" description={confirmingCurrent ? `確定將 ${confirmingCurrent.year} 設為目前學年度嗎？這會立即影響幹部後台權限。` : ""} confirmLabel="設為目前" /><ConfirmDialog open={deleting !== null} onClose={() => { if (!busy) setDeleting(null); }} onConfirm={remove} isSubmitting={busy} title="刪除學年度？" description={deleting ? `確定刪除 ${deleting.year} 學年度嗎？若已有社員資格或幹部職位資料，系統會拒絕刪除。` : ""} /></div>; }
