"use client";import{useState}from"react";import{useRouter}from"next/navigation";import{ConfirmDialog}from"@/components/ConfirmDialog";import{Button}from"@/components/ui/Button";import{apiClient}from"@/libs/api/client";import type{AttendanceRecord}from"./AttendanceActions";export function AttendanceDeleteAction({eventId,record}:{eventId:string;record:AttendanceRecord}){const r=useRouter(),[open,setOpen]=useState(false),[busy,setBusy]=useState(false);const del=async()=>{setBusy(true);try{await apiClient(`/api/admin/events/${eventId}/attendances/${record.id}`,{method:"DELETE"});setOpen(false);r.refresh()}finally{setBusy(false)}};return <><Button size="sm" variant="danger" onClick={()=>setOpen(true)}>刪除</Button><ConfirmDialog open={open} onClose={()=>!busy&&setOpen(false)} onConfirm={del} isSubmitting={busy} title="刪除簽到紀錄？" description="此操作無法復原。"/></>}
