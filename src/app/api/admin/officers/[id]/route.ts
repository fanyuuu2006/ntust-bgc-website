import { NextResponse } from "next/server";
import { getCurrentUser, isAdminByUserId } from "@/libs/auth";
import { officerPositionsService } from "@/services/officer-positions/officer-positions.service";
import { OfficerInputError, OfficerPositionNotFoundError } from "@/services/officer-positions/officer-positions.errors";
import { unexpectedErrorResponse } from "@/libs/api/server-response";
type Context = { params: Promise<{ id: string }> }; async function admin() { const user = await getCurrentUser(); return Boolean(user && await isAdminByUserId(user.id)); }
export async function PATCH(request: Request, { params }: Context) { if (!await admin()) return NextResponse.json({ message: "您沒有管理幹部職位的權限" }, { status: 403 }); try { return NextResponse.json({ data: await officerPositionsService.updateForAdmin((await params).id, await request.json()) }); } catch (error) { if (error instanceof OfficerInputError) return NextResponse.json({ message: error.message }, { status: 400 }); if (error instanceof OfficerPositionNotFoundError) return NextResponse.json({ message: error.message }, { status: 404 }); return unexpectedErrorResponse("[PATCH /api/admin/officers/[id]]", error, "更新幹部職位失敗，請稍後再試"); } }
export async function DELETE(_: Request, { params }: Context) { if (!await admin()) return NextResponse.json({ message: "您沒有管理幹部職位的權限" }, { status: 403 }); try { await officerPositionsService.deleteForAdmin((await params).id); return NextResponse.json({ data: null }); } catch (error) { if (error instanceof OfficerPositionNotFoundError) return NextResponse.json({ message: error.message }, { status: 404 }); return unexpectedErrorResponse("[DELETE /api/admin/officers/[id]]", error, "移除幹部職位失敗，請稍後再試"); } }
